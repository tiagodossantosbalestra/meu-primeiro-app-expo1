import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import {us fonts, montserrat-400regular,montserrat_700bold} from '@expo-google-fonts/montserrat';

export default function App() {

  const [fonte carregada] = use fonts({
    "mont seerratRegular":montserrat_400regular


  return (
 <SafeAreaView>
  <StatusBar />
 </SafeAreaView>
  );
}

